
# Tkinter Login Form

This is a simple GUI application built using Python's Tkinter library.  
The app allows users to enter a username and password and displays the entered data when the button is pressed.

## 📌 Features

- Simple and clean GUI
- Username and password input fields
- Button to display entered data
- Basic Tkinter widgets (Label, Entry, Button)

## 🛠️ Technologies Used

- Python 3
- Tkinter (Built-in Python GUI library)

## 🚀 How to Run

1. Make sure Python is installed on your system.
2. Save the file as `app.py`
3. Open terminal or command prompt.
4. Run:

```bash
